sess = gpt2.start tf sess() 
gpt2.fifinetune(sess, 
	7dataset=fifile name, 
	model name=’345M’, 
	steps=10000, 
	restore from=’latest’, 
	print every=10, 
	sample every=200, 
	save every=500 
) 


